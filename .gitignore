.env
.env.*
!.env.example

# Local Supabase overrides (safety net — never commit secrets)
assets/supabase-config.js
.vercel
.env*
output/

# Tooling — the deployable artifact is the .html files, not a build output
node_modules/
.artifacts/
